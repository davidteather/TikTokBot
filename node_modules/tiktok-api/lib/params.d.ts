import { ListRequestParams } from './types';
export declare const paramsOrder: Map<string, number>;
export declare const paramsSerializer: (order: Map<string, number>) => (params: object) => string;
export declare const withDefaultListParams: (params: ListRequestParams) => {
    count: number;
    retry_type: string;
};
